name: 04-using-actions

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: printin folders  
              run: ls

# 0. Checkout the code from the repository
# 1. Install deps of our react application
# 2. Execute automated tests